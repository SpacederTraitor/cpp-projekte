#include <iostream>

using namespace std;

int main()
{
    cout << "Binäre Suche" << endl;
    int array[10] = {1,2,3,4,5,6,7,8,9};
    int size = sizeof(array)/sizeof(array[0]);
    int target;
    cout << "Geben Sie den Wert ein";
    cin >> target;

    int left;
    int right = size -1;

    while (left <= right) {

    }
    return 0;
}
